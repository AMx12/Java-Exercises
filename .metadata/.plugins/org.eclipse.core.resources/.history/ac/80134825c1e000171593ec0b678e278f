
public class nTwice {

	public static void main(String[] args) {
		nTwice input = new nTwice();
		System.out.println(input.NTwice("Doubting", 3));
	}

	public String NTwice(String stringInput, int intInput) {
		String outputStart = "";
		String outputEnd = "";
		String outputTwice = "";

		for (int i = 0; i < intInput; i++) {
			outputStart += stringInput.charAt(i);
			outputEnd += stringInput.charAt((stringInput.length() - (intInput + 1)) + i);
		}

		outputTwice = outputStart + outputEnd;
		return outputTwice;
	}

}
